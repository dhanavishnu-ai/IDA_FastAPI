name: Trigger auto deployment for container-app-pmxf3qwefbnew

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/container-app-pmxf3qwefbnew-AutoDeployTrigger-039ff628-7939-4a9f-99d0-cdd8779166fc.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPPMXF3QWEFBNEW_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPPMXF3QWEFBNEW_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPPMXF3QWEFBNEW_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistrypmxf3qwefbnew.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPPMXF3QWEFBNEW_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPPMXF3QWEFBNEW_REGISTRY_PASSWORD }}
          containerAppName: container-app-pmxf3qwefbnew
          resourceGroup: idaappb3dc
          imageToBuild: containerregistrypmxf3qwefbnew.azurecr.io/container-app-pmxf3qwefbnew:${{ github.sha }}
          dockerfilePath: Dockerfile


